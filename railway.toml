[build]
buildCommand = "python WebApp/src/manage.py collectstatic --noinput"

[deploy]
startCommand = "cd WebApp/src && python manage.py migrate --noinput && python manage.py createcachetable && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"